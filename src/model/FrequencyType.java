package model;

public enum FrequencyType {
    DAILY,
    TWO_PER_WEEK,
    THREE_PER_WEEK,
    FOUR_PER_WEEK,
    FIVE_PER_WEEK,
    SIX_PER_WEEK,
    WEEKLY,
    // MONTHLY // will be implemented later
}
