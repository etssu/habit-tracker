package service;

public class TrackingService { // is used for marking tasks
    // edits the entry (if the user made a mistake)
    // receives a list of entries for a specific habit or period
}
